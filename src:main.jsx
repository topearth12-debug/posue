import React from "react";
import ReactDOM from "react-dom/client";
import PostureCheckApp from "./PostureCheckApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PostureCheckApp />
  </React.StrictMode>
);
